import React, { Fragment } from 'react';

import Header from '../../shared/header/Header';

const Home = () => {
    return (
        <Fragment>
            <Header />
        </Fragment>
    );
};

export default Home;
